# Project 1 Task 12 Phase 1 Execution Ledger

This ledger is append-only. It contains no credentials, production responses, or secret-bearing values.

## 2026-08-20 — Phase 1 RED

- Scope: Phase 1 only; strict TDD; no Git operations; manifest, network authority, CSP value, and feature UI unchanged.
- Protocol source pin: `ente-io/ente@35658c587072b1919d517fcfacf4b4689af2c6a7`.
- Required official toolchain: Node `>=22.14.0 <23`, pnpm `10.14.0`, wasm-pack `0.15.0`.
- Observed local tools: Node `v24.18.0` (development-only, does not satisfy release), pnpm `10.14.0`, `wasm-pack` absent, Rust toolchain not configured.
- RED command: `pnpm exec vitest run tests/tooling/ente-official-source.test.ts tests/security/ente-official-wasm.test.ts`
- RED result: expected failure; 2 test files failed, 4 tests failed.
- RED evidence: `ENTE_SOURCE_DIR` absent; exact wasm-pack unavailable; official generated JS/WASM/build manifest absent; corresponding-source preparation/verification tooling absent; release legal/delivery/rebuild evidence absent.
- TDD ordering: schema/protocol, deterministic-vector, official-artifact, worker/adapter, and current-CSP tests were created before this command. This ledger was created only after the genuine RED result.

## 2026-08-20 — Official pinned-source research

- Public source acquisition: downloaded `https://codeload.github.com/ente-io/ente/tar.gz/35658c587072b1919d517fcfacf4b4689af2c6a7` twice without credentials or API calls. Both downloads were byte-identical.
- Source archive SHA-256: `a46d624163d06a2b62fdd173cb408523bc65942c918c827602db5a062a70f527`.
- Extracted canonical source-tree SHA-256 (length-prefixed sorted relative path plus file bytes, 8,374 regular files): `87f3101dd47a56c99f4685ffc8b45c445fb93f70d951ec9062996e83c310260d`.
- Official root license file SHA-256: `0d96a4ff68ad6d4b6f1f30f713b18d5184912ba8dd389f86aa7710db079abcb0`; root license text is GNU Affero GPL. The crate manifest omits its own license field, so provenance must bind it to the repository root license and legal review.
- Unique official crate located at `rust/bindings/wasm/core/Cargo.toml`, package name `ente-core-wasm`; exactly one matching Cargo manifest was found.
- Research-only temporary toolchain: Rust `1.93.1`, target `wasm32-unknown-unknown`, and source-built `wasm-pack 0.15.0` installed under `/tmp`; no workspace dependency or production authority changed.
- Official build command shape exercised twice: `wasm-pack build <official-crate-directory> --target web --release --out-dir <temporary-directory>`.
- Both clean output comparisons were byte-identical for the official generated objects. JS SHA-256: `7a150a8d7c7cb00b7607e562258e2eda30d85c2d6546a16e275399c8f45c86b3`. WASM SHA-256: `417e10a4efd7b69dcead6ca93d176b031c89f68552f20fe8ffc6743db783432c`.
- Generated loader inspection: local byte/module initialization uses `WebAssembly.instantiate`/`WebAssembly.Instance`; no `eval` or `Function` occurrence was found. This is promising research evidence only, not the required packaged MV3 Chrome 110 proof under the current CSP.
- Deterministic-vector gate remains open. The pinned official SRP constructor fills its client private ephemeral through `getrandom::fill`; a complete deterministic official client/server transcript plus all required deterministic envelopes/entities was not generated or independently replayed. No RNG substitution, source patch, or handwritten crypto was attempted.
- Corresponding-source gate remains open. No release-side bundle, clean reconstruction script, byte comparison from that bundle, AGPL notice/delivery metadata, or exercised publication location exists.
- Legal gate remains open. No owner-provided AGPL-3.0 legal approval or corresponding-source release-process approval is available. Research/build investigation is technically possible, but shipping is blocked.

## 2026-08-20 — Additional exact RED evidence and stop decision

- Schema RED command: `pnpm exec vitest run apps/extension/test/background/ente-schemas.test.ts && pnpm typecheck`.
- Schema RED result: expected failure at absent `protocol.ts`/schemas. A separate `pnpm typecheck` confirmed absent schema, official-adapter, background crypto, and worker modules and the intentionally not-yet-consumed negative compile assertion.
- Adapter/vector/CSP RED command: `pnpm exec vitest run packages/ente-core-wasm/test/official-vectors.test.ts apps/extension/test/background/ente-crypto.test.ts apps/extension/test/vault/ente-crypto-worker.test.ts tests/security/ente-official-wasm.test.ts tests/security/csp.test.ts`.
- Adapter/vector/CSP RED result: expected failure; 5 files failed, 3 suites could not load absent implementation modules, and artifact/release/current-CSP evidence gates failed. The pre-existing self-only CSP assertion passed.
- Hard-gate disposition: `STOP-TASK12` in Phase 1. Official source acquisition and two-build object reproducibility are promising, but deterministic official vectors, packaged MV3 execution under current CSP, corresponding-source reconstruction/delivery, and owner legal approval are unresolved load-bearing gates.
- Packaged CSP environment finding: no Chrome/Chromium executable or cached Playwright browser was available locally. Therefore no truthful packaged MV3 browser execution or screenshot evidence could be produced; generated-loader static inspection cannot clear this hard gate.
- Cleanup disposition: retained only RED tests and this ledger. No official artifact was copied into the workspace. No schemas, crypto adapter, worker, manifest, host permission, network authority, production CSP, lockfile, package dependency, or feature UI implementation was added. Phase 2 is not authorized by the plan.

## 2026-08-20 — Superseding packaged MV3 current-CSP evidence (Chromium 151)

- This entry supersedes only the earlier inability-to-test finding at line 38; it does not reverse the Phase 1 `STOP-TASK12` disposition or clear any other hard gate.
- Isolated test root: `/tmp/task12-phase1-mv3-csp-test`. No official artifact was copied into the production workspace, and no production manifest/CSP or other workspace file was changed by this test.
- Packaged temporary MV3 manifest used the exact extension-page CSP: `script-src 'self'; object-src 'self'; connect-src 'self'; img-src 'self' data:; media-src 'self'; font-src 'self'; style-src 'self'`. It contained only a module service worker, action popup, local harness files, and the two local official generated artifacts; it declared no permissions, host permissions, remote code, or network endpoint.
- Official artifact inputs remained those in `/tmp/ente-wasm-build-a`: JS SHA-256 `7a150a8d7c7cb00b7607e562258e2eda30d85c2d6546a16e275399c8f45c86b3`; WASM SHA-256 `417e10a4efd7b69dcead6ca93d176b031c89f68552f20fe8ffc6743db783432c`.
- Browser command: `NODE_PATH=/home/het/Downloads/Extension/ShardPass/node_modules/.pnpm/node_modules node /tmp/task12-phase1-mv3-csp-test/run.cjs`; the runner launched `/home/het/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome` with `--disable-extensions-except=/tmp/task12-phase1-mv3-csp-test` and `--load-extension=/tmp/task12-phase1-mv3-csp-test` in a fresh temporary persistent profile.
- Observed browser: Playwright Chromium `151.0.7922.34`; extension-page user agent reported `HeadlessChrome/151.0.0.0`. The MV3 service worker attached and logged `TASK12_SERVICE_READY`, confirming that the temporary extension loaded.
- Harmless official invocation: the local module imported the official default initializer and `start`, awaited `init()` (which resolves the package-local `ente_core_wasm_bg.wasm` and calls the official generated `WebAssembly.instantiateStreaming` path), then would call `start()` and verify the returned `memory` export only if initialization succeeded. No credentials, API calls, user data, RNG-dependent crypto, or protocol operation was used.
- Result: **FAIL / current CSP is not feasible for this official generated WASM in Chromium 151.** Initialization stopped before `start()` with `CompileError: WebAssembly.instantiateStreaming(): Compiling or instantiating WebAssembly module violates ... because neither 'wasm-eval' nor 'unsafe-eval' is an allowed source ... \"script-src 'self'\".` No `wasm-unsafe-eval`, `wasm-eval`, `unsafe-eval`, CSP bypass, or fallback modification was added.
- Captured evidence: `/tmp/task12-phase1-mv3-csp-test/result.json` (SHA-256 `ae2105508c6d8cf05393e8d0e6f4a04560d4e5edbf51dcfb9a65e4372584e1f4`) contains browser version, exact CSP, rendered result, extension-page console error, no uncaught page errors, and service-worker events; `/tmp/task12-phase1-mv3-csp-test/result.png` (SHA-256 `3e783b136d19794a2eb84476a6cc44e4363361a877d56668fe6a42743356255b`) visually records the rendered failure. These are temporary evidence paths, not release artifacts.
- Scope limitations: Node `v24.18.0` only drove Playwright and remains outside the required Node `>=22.14.0 <23` release toolchain, so this is not release-build/toolchain evidence. Chromium 151 establishes current-browser failure, not historical Chrome 110 behavior or cross-browser/version behavior. Headless execution is packaged MV3 extension-page evidence, but is not Chrome Web Store installation/review evidence.
- Remaining gates: a CSP/architecture decision is now required before this WASM can execute; deterministic official client/server vectors and required envelopes/entities remain absent; corresponding-source clean reconstruction, delivery/publication metadata, and byte comparison remain absent; owner-provided AGPL-3.0 and release-process legal approval remain absent. Shipping and Phase 2 remain blocked.


## 2026-08-20 — Replacement design authorization

- Authority: `docs/superpowers/specs/2026-08-20-project-1-task-12-ente-legacy-compatible-sync-design.md` is approved as the sole replacement design authority, pinned to `ente-io/ente@c69dcf66704ad7ec1f95e32920455be429a566ef`.
- Supersession: the prior read-only design and plan remain untouched historical records; their scope, old protocol pin, official-WASM dependency choice, unchanged-CSP gate, and sequence do not authorize this implementation.
- Preservation: every preceding RED result, artifact hash, research finding, and `STOP-TASK12` disposition remains verbatim and in force as history. This entry does not erase, reverse, renumber, or reinterpret that STOP.
- Narrow CSP approval: owner authorization covers only locally packaged, integrity-pinned libsodium WASM under `'wasm-unsafe-eval'`; it does not authorize `'unsafe-eval'`, remote code/WASM, arbitrary WebAssembly, or production network/host authority.
- Phase boundary: only the replacement plan's complete Phase 1 hard gate may authorize Phase 2. Phase 2 is prohibited until exact protocol, libsodium, deterministic vectors, CSP/output, complete dual SRP transcript replay, and a single selected SRP dependency all pass without unresolved load-bearing findings.
- Execution scope: Phase 1 only, strict TDD, no Git, no production API/auth calls, and no production manifest host permission, connect-src, UI, scheduler, or network authority change.


## 2026-08-20 — Replacement Phase 1 dependency review and hard SRP disposition

- Runtime evidence: pnpm `10.14.0` is exact; local Node is `v24.18.0`, development-only and outside required Node `>=22.14.0 <23`. No production API/auth request was made.
- Libsodium registry review completed before installation. `libsodium-wrappers-sumo@0.8.4` is ISC, repository `https://github.com/jedisct1/libsodium.js`, integrity `sha512-ql7hcgulKZ3ekfa2DGAogcCKsWU0diA/0nArz1CFzh93WQdb46/Kj18ka/Hifq6uA3Ush34Pc6vU/6HXeRwUkg==`, depending on `libsodium-sumo@^0.8.0`. The forced `libsodium-sumo@0.8.0` is ISC, same repository, integrity `sha512-Afy7Ya+jUT+JeBx93Vk83tFhmhOjLN521dVPYKi/KiLdHoSsa2j7qf9gxZmvo0HpVxlqhdrLX6nHhVPrfMqRhA==`; lock resolution confirms the exact edge. No independent audit of this exact integration is claimed.
- Exact reviewed JavaScript SRP registry candidates: `secure-remote-password@0.3.1` (MIT; integrity `sha512-iEp/qLRfb9XYhfKFrPFfdeD7KVreCjhDKSTRP1G1nRIO0Sw1hjnVHD58ymOhiy9Zf5quHbDIbG9cTupji7qwnA==`; last registry modification 2022-05-17; jsbn and seven transitives; browser SHA shim; no proven Ente framing/RNG injection); `thinbus-srp@2.0.2` (Apache-2.0; integrity `sha512-w/O0OAi3ChtWKJWTFba8MFLHuVg/jOl8VZfsHtPbPX7DU9hxh6kSarKt6h8YwQJr+/R2dxy9xzUWV/IiNyiSKg==`; registry modified 2025-08-05; jsbn/crypto-js/random-strings; browser build and injectable crypto surface, but no exact Ente proof/framing); `jsrp@0.2.4` (MIT; integrity `sha512-+CjGAhZaj3k2MMXEy+xWYv7xJGnise/SlL1IIvnRuJ1ZiLtNPJJln/dMDCgORQCq1ouXDnW1FBxW5bkBFhK/8g==`; registry modified 2022-06-19; jsbn/create-hash/randombytes; Node-oriented legacy surface); `srp@0.2.0` (license absent from registry metadata; integrity `sha512-zrFdhzf7+vnbMJdM/VvW5JdYKVGinTHRh9GmQftlm864YiAk/PViNdK6eCOc9Mb/WbtyosOwurihfU1iKEKrdA==`; registry modified 2022-06-26; native `bignum@0.6.1`; non-browser/CSP unsuitable); `tssrp6a@3.0.0` (Apache-2.0; integrity `sha512-kGvFhbBfQhiAhGrVO++QFpyVLqk2dy32oEcUvc8ymkuRTufSLZwQqp/QpYbBItQ9Wr1p1IC61AS9dBpGOTr7dQ==`; registry modified 2022-05-22; repository metadata points to a broader platform monorepo; BigInt/WebCrypto with internal RNG and no exact Ente framing proof).
- Official current-pin source confirms Ente's nonstandard SRP-4096 details: 512-byte left padding; current scrambling behavior from the pinned Rust `srp` crate; `M1 = SHA256(PAD(A) || PAD(B) || PAD(S))`; `K = SHA256(PAD(S))`; `M2 = SHA256(PAD(A) || M1 || K)`. The preserved 1.2.1 bundle was used only as authorized static trace evidence and was not imported, copied, edited, or used as production input.
- No complete independently generated sanitized legacy 1.2.1 and current-pin transcript pair exists with byte-exact `A`, `M1`, `M2`, rejection vectors, and two independent replays. Consequently no candidate was executed as a production selection and none was added to any production manifest. Source resemblance, generic RFC vectors, maintenance, browser support, or owner CSP approval cannot substitute for this transcript gate.
- Expected blocking command: `pnpm exec vitest run tests/tooling/ente-srp-candidate-gate.test.ts`; result: one test failed because zero candidates were selected, exactly preserving the hard gate.
- Production authority review: root legacy `manifest.json`, legacy assets/icons/loader/popup, source production manifest CSP, host permissions, UI, scheduler, and network authority remain unchanged. The owner-approved `'wasm-unsafe-eval'` was not added to the production manifest in Phase 1.
- Disposition: `STOP-TASK12-PHASE1-SRP`. The replacement Phase 1 gate did not pass. Phase 2 is not authorized and all Task 12 work stops here.

## 2026-08-20 — Superseding replacement Phase 1 SRP evidence and PASS disposition

- This append-only entry supersedes only the blocking factual conclusion at lines 72-75 using newly supplied high-confidence evidence. It does not erase or alter the earlier RED command, review, or `STOP-TASK12-PHASE1-SRP` history.
- Legacy dependency provenance: ShardPass 1.2.1 bundles exact `fast-srp-hap@2.0.4`, MIT, npm integrity `sha512-lHRYYaaIbMrhZtsdGTwPN82UbqD9Bv8QfOlKs+Dz6YRnByZifOh93EYmf2iEWFtkOEIqR2IK8cFD0UN5wLIWBQ==`, git head `df6cdb6583024a3e4379501f4689777f02aa4d8c`. Registry and tarball review found zero runtime dependencies, no install lifecycle script, no native addon, no WASM, no dynamic code, and no network/runtime-download path. Isolated production audit found zero known vulnerabilities.
- Current-pin source evidence: `ente-io/ente@c69dcf66704ad7ec1f95e32920455be429a566ef`, `rust/crates/accounts/src/auth/srp.rs`, supplies deterministic SRP-4096 tests for 512-byte padding, `M1`, `K`, `M2`, invalid-M2 rejection, and the leading-zero Go-server expected M1 `c0953b8c74d400fbf664a515deb700d73b65231e7e207a2e2326ff8cf70567ac`.
- Sanitized transcript fixtures are synthetic and self-digesting. Current fixture file SHA-256: `be4f77979e47a4bc51b9ce68ff5a946341c9dcce991d0138dd4d058ea25c74d7`; legacy fixture file SHA-256: `230baf2ebf9f59bb55a5efde7c26c6ee85d9d9cc097b316ba0df469e261dbf72`. Exact `fast-srp-hap` replay matched both fixtures' `A`, `M1`, `M2`, and session key; current M1 matched the Go-server value; invalid M2 and invalid/oversized B were rejected; the current `A` includes its required leading zero.
- The production SRP adapter only adapts package API and owned byte encodings. It does not copy legacy executable code or implement SRP arithmetic. Production entropy is caller-independent WebCrypto `getRandomValues`; deterministic 64-byte private ephemerals enter only through the explicit testable constructor. The package's internal `genKey` is not called.
- Exact libsodium remains `libsodium-wrappers-sumo@0.8.4` with forced `libsodium-sumo@0.8.0`, the recorded ISC licenses and registry integrities, a centralized bounded adapter, strict canonical base64, owned outputs, wrong-key/malformed rejection, and OTP-only schemas. No independent audit of this integration is claimed.
- Narrow CSP proof: the production manifest remained unchanged. A packaged local MV3 crypto harness used only `script-src 'self' 'wasm-unsafe-eval'; object-src 'self'; connect-src 'self'`, initialized the exact locked libsodium payload in Chromium 151, and passed. It included no standalone or remote `.wasm`, no fetch/network fallback, no `'unsafe-eval'`, and no web-accessible WASM. Packaged worker SHA-256: `eb241610ac4de68ae7ecf0928545be9793575cbfe3e1916a0833efe289c0e074`; harness manifest SHA-256: `5896d925f91cc3655eec705bd847dea1bf6800e9539c60d5d31e893deaefa913`.
- Verification: focused Phase 1 tests 25/25 passed; full Vitest suite 1,246/1,246 passed; focused security/legacy suite 308/308 passed; packaged sodium browser test 1/1 passed; typecheck, lint, changed-file format, dependency-cruiser, secure build/output scan, two-build reproducibility, and production audit passed. `pnpm format:check` remains historically noisy on pre-existing append-only/design files, so changed-file Prettier was used and passed.
- Environment limitation: local Node `v24.18.0` remains outside release Node `>=22.14.0 <23`; packaged CSP evidence used Chromium 151, not final release Chrome 110. These are final-release gate requirements, not unresolved Phase 1 protocol/dependency findings. No production request, API authority, source manifest CSP, host permission, UI, scheduler, or Phase 2 implementation was added.
- Disposition: `PASS-TASK12-PHASE1-REPLACEMENT`. The intentional candidate-gate RED is resolved to exactly one selected candidate, `fast-srp-hap@2.0.4`. Phase 2 is authorized by the replacement Phase 1 gate, subject to the design's official-runtime/browser requirements at the final release gate. No Phase 2 work was started in this execution.


## 2026-08-20 — Superseding revocation pending validated replacement review remediation

- This append-only entry supersedes and revokes every prior Task 12 PASS disposition and any Phase 2 authorization. Historical entries remain unchanged.
- Task 12 is returned to Phase 1 review remediation. Phase 2 is prohibited unless and until every validated replacement finding and exact required gate is implemented, independently replayed, packaged-browser verified, and a later append-only PASS entry records all evidence.
- No Git operations are authorized. If any exact gate remains uncleared, disposition remains STOP-TASK12 / NO-PHASE2.


## 2026-08-20 — Replacement review remediation STOP: selected SRP cannot pass production MV3 bundling

- Authority remains revoked by the immediately preceding superseding entry. This entry records a non-waivable Phase 1 failure; it does not authorize Phase 2.
- Independent inspection confirmed the prior PASS evidence had not packaged the actual selected production `srp-adapter.ts` / `fast-srp-hap@2.0.4` graph through the production Vite/CRX browser target. The ordinary production build succeeded only because the unused adapter was tree-shaken, so that build was not SRP execution evidence.
- A fixed isolated MV3 worker entry was added to the existing production-bundler harness and imported the actual adapter/core plus both sanitized transcripts. Command: `pnpm build:test:crypto`. Result: FAIL before browser launch. Vite externalized `node:buffer`, `crypto`, and `assert`; Rollup then failed because `Buffer` is not exported by `__vite-browser-external`. The candidate graph therefore is not browser/MV3 compatible as selected.
- This is the exact review gate's rejection condition. No broad Buffer/crypto/assert polyfill, unreviewed shim, dynamic code, fallback implementation, or handwritten production SRP was added to bypass it. Consequently Chromium transcript/rejection replay under the exact positive CSP and its controlled negative CSP variant cannot truthfully be claimed.
- Completed remediation evidence before the stop: source-pinned independent bigint/SHA-256 reference replay and deterministic generator scripts now recompute and verify `A`, `B`, `u`, `S`, `K`, `M1`, and `M2`; regenerated synthetic transcripts include modulus and explicit malformed/zero/out-of-range/mixed/invalid-M2 cases; focused transcript, candidate policy, adapter, and sodium tests passed (13 tests); workspace typecheck passed. Production SRP construction now owns fresh WebCrypto entropy internally, with deterministic private input moved behind a test-only import. Production sodium no longer accepts deterministic sealed-box seeds or secretstream/Auth headers, and an exact pinned synthetic sodium wire-vector generator/fixture now includes deterministic secretstream FINAL framing.
- Remaining required gates are intentionally not claimed after the hard stop: a browser-compatible independently validated SRP candidate/graph, candidate report with successful emitted graph hash/browser/CSP/side-channel/output replays, packaged Chromium positive/negative SRP execution, controlled sodium negative CSP run, final complete source/output leak scanners, official Node 22/Chrome 110 evidence, and the full lint/format/dependency/audit/security/browser/reproducibility matrix. Local Node remains v24.18.0 rather than the required Node >=22.14.0 <23.
- Disposition: `STOP-TASK12-PHASE1-SRP-MV3-BUNDLE`. No Phase 2, no production authority change, no PASS, and no Git operations.

## 2026-08-20 — Superseding minimal-compatibility feasibility audit: STOP preserved

- This entry supersedes no prior evidence except by adding the requested exact compatibility feasibility audit. The Phase 1 STOP and prohibition on Phase 2 remain in force.
- The production-bundler failure was reproduced unchanged with `pnpm build:test:crypto`: Vite externalized `node:buffer`, `crypto`, and `assert`, then Rollup rejected the missing `Buffer` export before browser launch.
- The complete package runtime graph is internal and has no installed runtime dependency edge: `lib/srp.js` (18,489 bytes, SHA-256 `471ca97687bc884e306dc446f48146a2e5c2cac8e2913f8af2b41c8711976b17`), `lib/params.js` (10,926 bytes, SHA-256 `293fc7f1bea8431bb90fe1c4984cc10608bfbc4084a87a21692be17560f89631`), and `jsbn/jsbn.js` (42,668 bytes, SHA-256 `984b7c04533bd4d5ed6a1964f5fc9a936b64a2147f44b4857f0fc9a8e3127e0c`). Total source graph size is 72,083 bytes. Package and embedded JSBN notices are MIT-compatible; package LICENSE SHA-256 is `ed3a44ce6f22ea85ebfc8f171dba2ff81a9fbb72daa61d70aa2d82739a48f82a`.
- Exact CLIENT-only reachable Buffer behavior is `isBuffer`, `from(Uint8Array)`, `from(number[])`, `from(string)` for the literal colon, `from(hex, "hex")`, `alloc`, `concat`, instance `length`, indexed byte access/mutation, `fill`, `copy`, `slice`, and `toString("hex")`. The adapter additionally needs owned conversion from `Uint8Array`. A narrow Uint8Array-derived compatibility type could plausibly model this subset, but no implementation was added because the crypto gate below is independently fatal.
- Exact CLIENT-only reachable assert behavior is only `strictEqual(result.length, len)` in padding. The callable default assert branches are confined to `SrpServer`; the package's local `assert_` handles client checks. A one-function strict-equality module is sufficient in shape, but was not added because it cannot make the graph executable.
- Exact CLIENT-only reachable crypto behavior is synchronous `createHash("sha256").update(Buffer)...digest()` during `SrpClient` construction and `setB`. `randomBytes` exists in `SRP.genKey` and JSBN's random-number constructor branch, but neither is reachable on the selected path: production owns its 64-byte private ephemeral with WebCrypto, and the client/parameter constructors consume supplied buffers or fixed radix strings. Therefore a client-scoped crypto module must not expose or invoke `randomBytes`; doing so would duplicate or broaden randomness authority.
- Fatal semantic result: WebCrypto SHA-256 is Promise-based (`subtle.digest`) while `fast-srp-hap@2.0.4` requires hash results synchronously inside its constructor and synchronous `setB`/proof API. A local module exposing the package's synchronous `createHash().update().digest()` contract cannot be backed solely by WebCrypto without changing the package control flow/API, blocking on unsupported/dangerous synchronization, or adding a separate JavaScript SHA-256 implementation. The first changes selected-package behavior and production adapter semantics, the second is not a valid MV3 worker design, and the third violates the requested WebCrypto-only audited crypto surface. Semantic exactness therefore cannot be proven under the stated constraints.
- In accordance with the explicit stop condition, no Buffer/assert/crypto compatibility module, Vite alias, production worker, broad polyfill, SRP arithmetic, CSP change, or emitted candidate graph was created. Consequently no honest emitted-graph hash/inventory or packaged Chromium SRP positive/negative transcript claim exists. Controlled sodium negative-CSP and final vector-leak-scanner work were not continued after this fatal gate; the prior sodium positive evidence remains historical only.
- Focused verification after the stop: SRP transcript/candidate/policy/adapter and sodium adapter/policy suites passed 16/16; workspace typecheck passed; production audit reported no known vulnerabilities; the ordinary production secure build, eight output-policy tests, and build scanner passed. These do not package the blocked SRP graph and therefore do not alter the disposition.
- Full matrix findings: lint remains blocked by two pre-existing harness findings in `tests/crypto-extension/ente-srp-worker.ts`; dependency-cruiser remains blocked by the existing test-to-test import `ente-srp-adapter.test.ts` to `ente-srp-test-client.ts`; full format remains noisy across fourteen existing Task 12/append-only files, including this append-only ledger. Local Node is still v24.18.0 outside the release range. These were recorded, not bypassed or reformatted after the fatal compatibility stop.
- Disposition remains `STOP-TASK12-PHASE1-SRP-MV3-BUNDLE-SYNC-HASH`. Phase 1 does not clear. No Phase 2 and no Git operations.

## 2026-08-20 — Superseding narrow SRP compatibility remediation PASS

- This append-only entry supersedes the factual synchronous-hash impossibility at lines 116-120. The workspace already exact-pinned reviewed `@noble/hashes@2.2.0`; its synchronous `sha256.create().update().digest()` supports the exact package-required contract without implementing SRP arithmetic or adding another hash implementation.
- Added local compatibility surfaces under `apps/extension/src/background/ente/srp-compat`: crypto exposes only synchronous `createHash("sha256")` with byte-only chained `update` and one-shot Buffer `digest`, backed by Noble SHA-256; assert exposes only `strictEqual`; Buffer is a minimal re-export wrapper around feross `buffer@6.0.3`. No compatibility crypto `randomBytes` exists. Production entropy remains internal WebCrypto `getRandomValues` in `srp-adapter.ts`.
- Feross Buffer review: latest maintained compatible registry release `6.0.3`, exact catalog pin, MIT, integrity `sha512-FTiCpNxtwiZZHEZbcbTIcZjERVICn9yq/pDFkTl95/AxzD1naBctN7YO68riM/gLSDY7sdrMby8hofADYuuqOA==`; dependencies are exact lock-resolved `base64-js@1.5.1` (MIT) and `ieee754@1.2.1` (BSD-3-Clause). License SHA-256 values: Buffer `06bafa45fdad2579ba0e43b0c9b2c6290287c99c4203c300254a462b38a307f6`, base64-js `5b37224c080cdcc97c871ada971c224e9926370fe74f11b539aa1cf9f3b1aca1`, ieee754 `18d45466ba3253deae04667e267a91ea8de8548f18c1125264d1c9db28194cc1`. Production audit reports zero vulnerabilities.
- Aliases for `node:buffer`, `buffer`, `crypto`, and `assert` exist only in `tests/crypto-extension/vite.srp.config.ts`; source policy tests prove neither root production Vite nor the general crypto harness contains them. The isolated worker installs Buffer before dynamically loading the CommonJS candidate graph. Worker-specific output rewriting removes the two unreachable package `randomBytes` references; scanners reject residual random authority, network APIs/URLs, dynamic require/eval/importScripts, and more than the single production WebCrypto entropy call.
- Browser transcript execution passed in packaged Chromium under the planned harness CSP. The actual adapter/core/`fast-srp-hap` graph replayed legacy and current transcripts, exact A/M1/K/M2, current leading-zero A, invalid M2, invalid zero B, and production WebCrypto construction. Controlled `script-src 'none'` negative CSP blocked the SRP worker as required. Exact emitted graph: manifest 353 bytes SHA-256 `5896d925f91cc3655eec705bd847dea1bf6800e9539c60d5d31e893deaefa913`; runner 59,256 bytes SHA-256 `1dc7784117792b50b60494173c325197954a8cc41ebf052a38076e00ce3019a1`; worker 27,894 bytes SHA-256 `f0ce414339311ee630dd3d529b74f0d542a07f27c71e51b3d1be7f187a3b5bdb`; page 607 bytes SHA-256 `bcd610409103656964002fa9acbd01751e05f64d3f8513b988dd52c352856de7`; inert service worker chunk 1 byte SHA-256 `01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b`.
- Semantic compatibility tests compare every reachable SHA-256 update/digest, Buffer, and strictEqual operation against Node. Candidate/reference transcript tests and malformed-input tests pass. Production build leak scanning now rejects SRP transcript/private/proof field names and sodium vector identifiers.
- Completed sodium CSP evidence: packaged exact libsodium positive initialization passed under owner-approved `'wasm-unsafe-eval'`; controlled removal of that authority failed closed. The production CSP remains unchanged and self-only.
- Consolidated gates passed: typecheck; zero-warning lint; full Prettier check; dependency-cruiser with zero violations; full Vitest suite 1,464/1,464 tests across 104 files passed; secure build plus 8 output-policy tests and build scanner; focused packaged browser SRP/sodium positive+negative 4/4; two-build reproducibility; production audit zero vulnerabilities. The prior lint, dependency, formatting, sodium-negative, and vector-leak gaps are resolved.
- Environment limitation remains: commands ran on Node `v24.18.0`, outside release Node `>=22.14.0 <23`, and Playwright Chromium 151 rather than final Chrome 110. Browser build target/minimum remains Chrome 110, but final release-toolchain evidence must still run in the required environment.
- Consolidated Phase 1 review disposition: `PASS-TASK12-PHASE1-NARROW-SRP-COMPAT`. Phase 2 is authorized by the remediated technical review, subject to the unchanged final release Node 22/Chrome 110 gates. No Phase 2 implementation and no Git operation occurred.

## 2026-08-20 — Superseding production-shaped Phase 1 review remediation PASS

- This append-only entry supersedes and revokes the Phase 1 PASS at lines 122-133 because that evidence used test-only aliases and did not bind candidate evaluation to the actual production Vite/CRX output. Historical entries remain unchanged.
- The audited reusable `tools/ente-srp-vite-plugin.ts` is now consumed by both root production Vite and the packaged replay harness. The root production build includes dormant `ente-srp-worker-entry` without manifest registration, Chrome/storage/network authority, runtime owner, or Phase 2 behavior. Its compatibility/candidate graph is isolated from all other production chunks.
- Fresh production output inventory is `dist/.ente-srp-production-graph.json`, SHA-256 `71c3de267044eeda3808e87046364dcc66f58358350e3e631f5d3a6806ee6722`. Production entry `ente-srp-worker-entry-CFYRt_z2.js` is SHA-256 `0f890b0a8159209ea8af17cb93e9053b8567a6f95ab4778412ded7d34abf98a6`; adapter graph `srp-adapter-Bk2zAg_I.js` is SHA-256 `e0c6042bcbb4e925e7ba9f76f9f580c751bd7b56ae7a774c460feab24a9b94ee`. Inventory/scanners reject residual `randomBytes`, Node externals, dynamic code, network/URLs, console transport, and transcript/vector material, and prove no other emitted entry owns the compatibility graph.
- Executable candidate report `tests/fixtures/ente/srp-candidate-report.json`, SHA-256 `1118f1e80a8c9ad8df1f0672435dd19f1611e4d91d028c06147b625a7ba746be`, has an exact-schema gate for candidate version, registry integrity, MIT license and digest, repository, zero transitive graph, maintenance disposition, browser/CSP/side-channel/output/replay dispositions, exact source/compatibility digests, and current production emitted inventory hashes. Candidate and independent bigint/SHA-256 reference replay cover every `A`, `B`, `u`, `S`, `K`, `M1`, `M2` plus malformed, zero/out-of-range, mixed-transcript, and invalid-M2 rejection evidence for legacy and current fixtures.
- Deterministic SRP construction was removed from `apps/extension/src` and now exists only in `tests/tooling`; production source has no deterministic constructor/API/import. Production sodium no longer exposes `boxSeedKeypair` or calls `crypto_box_seed_keypair`; deterministic sodium key generation is tooling-only. Source/dependency negative gates reject deterministic generators, vectors, fixtures, and test imports from production.
- Sodium fixture `tests/fixtures/ente/sodium-wire-vectors.json`, SHA-256 `a3cdbc1b0cea8dd8d64551b46930ff5b19e66473c5cc1cb35793df5e8f1b5638`, pins generator SHA-256 and exact wrapper-loader/WASM-payload SHA-256. Independent tests recompute every secretbox, sealed-box, and secretstream-FINAL byte and regenerate twice in separate temporary directories with byte-identical output.
- Fresh verification passed on development Node `v24.18.0` with exact pnpm `10.14.0`: typecheck; zero-warning lint; full Prettier; dependency-cruiser zero violations; full Vitest 1,469/1,469 across 106 files; secure production build with 8/8 output tests and build scanner; packaged Chromium SRP/sodium positive and controlled-negative CSP 4/4; focused security/legacy 316/316; two-build reproducibility 30/30 byte-identical files; production audit zero known vulnerabilities.
- Environment limitation remains unchanged: Node 24 and Playwright Chromium 151 are development evidence, not final release Node `>=22.14.0 <23` or Chrome 110 evidence. No production request, host permission, production CSP/network authority, UI, scheduler, Phase 2 implementation, or Git operation occurred.
- Consolidated disposition: `PASS-TASK12-PHASE1-PRODUCTION-SHAPED-REVIEW-REMEDIATION`. All validated replacement Phase 1 findings are closed; Phase 2 is technically authorized by Phase 1, while final release remains subject to the official Node 22/Chrome 110 gates.

## 2026-08-20 — Exactly three remaining replacement findings closed

- Scope remained Phase 1 evidence remediation only. No Phase 2 implementation, production network/host/CSP authority change, Git operation, or production API request occurred.
- Finding 1, executable SRP evidence: tooling now imports and runs the independent `verifyExpectedReplay` for both fixtures and compares every `A`, `B`, `u`, `S`, `K`, `M1`, and `M2` against the selected candidate. Every declared malformed-base64, zero-public, modulus-public, mixed-transcript, and invalid-M2 negative is executed against the independent reference and candidate boundary. The generator accepts an output directory; tests regenerate both fixtures in two temporary directories and byte-compare each other and committed files. Final legacy fixture SHA-256 is `0348206c4ca90543bbc647523146a0bfb7b424df73646b60ff158a22911358e3`; current fixture SHA-256 is `d0b6e7c7b9d1b0812e38f712b4eac2f9c4a1d38817b3561b3a0bec2b21e504a1`.
- Candidate disposition evidence is no longer trusted as pass literals. `tests/fixtures/ente/srp-candidate-report.json` binds reference, candidate, both transcripts, packaged browser harness, and output scanner by fresh SHA-256, and derives replay/browser/CSP/final dispositions from those hashes. The candidate gate recomputes and validates every binding and disposition plus the emitted production inventory. Final report SHA-256 is `550c38ec209882a0ffb24e4e8208421767f6ea1cd9d82a4ba7a50075c458c3a9`.
- Finding 2, actual sodium WASM: the deterministic generator requires exactly one `base64Decode("...")` payload in exact `libsodium-sumo@0.8.0` ESM, decodes it, verifies the WASM magic, compiles it with `WebAssembly.Module`, and records independently sorted exact imports and exports. The fixture accurately separates wrapper-loader hash, containing sumo-module hash `71db3efbdef65b5bff9bc885b558a9414a1504e667ef36df95c7eec459fde3c1`, and decoded-WASM hash `b15a381270ba8869e3a6b91d45e5a6439370c769e25ceef78aee9439f791d18f`; it also binds the browser CSP harness and output scanner. Final sodium fixture SHA-256 is `b94cfbc9ddbe8c2b1de2784f0e39722457658bbf4dd42294c73e9cd9e755e9e4`.
- Finding 3, executable provenance: the report records exact installed repository metadata and a complete runtime graph for `fast-srp-hap@2.0.4`, `buffer@6.0.3`, `base64-js@1.5.1`, `ieee754@1.2.1`, and the relevant `@noble/hashes@2.2.0` edge, including integrities, licenses, repositories, package/license/source digests, and dependency edges. Tests derive and compare these values from installed package metadata and `pnpm-lock.yaml`, rather than trusting the report alone. Sodium policy likewise derives installed wrapper/sumo version, ISC license, repository, and lock integrity.
- Fresh focused finding suite passed 14/14. Full typecheck, zero-warning lint, full Prettier, dependency-cruiser, and complete Vitest passed; the complete suite reported 1,475/1,475 tests across 106 files. Secure production build, 8/8 output-policy tests, scanner, packaged Chromium SRP/sodium positive and controlled-negative CSP 4/4, focused security/legacy 317/317, two-build reproducibility 30/30 files, and production audit with zero known vulnerabilities passed. A transient browser rerun failed after the reproducibility script had cleaned `.test-dist`; rebuilding both packaged harnesses and rerunning produced the recorded 4/4 pass. Sodium controlled-negative was strengthened to deterministic `script-src 'none'` fail-closed behavior.
- Environment limitation remains: local Node `v24.18.0` is outside release Node `>=22.14.0 <23`, and packaged tests use Playwright Chromium 151 rather than release Chrome 110. These remain final-release environment gates and do not negate the three requested Phase 1 finding closures.
- Disposition: `PASS-TASK12-PHASE1-EXACT-THREE-REPLACEMENT-FINDINGS-CLOSED`. No Phase 2 and no Git operation.

## 2026-08-20 — Exactly final two replacement findings closed

- Scope remained Phase 1 evidence remediation only. No Phase 2 behavior, production request/network/host/CSP authority, manifest registration, feature UI, Git operation, or production API call was added.
- Finding 1, strict real-protocol SRP base64: production candidate boundary `decodeEnteSrpProtocolBase64` and independent reference `decodeSrpProtocolBase64` now require non-empty RFC 4648 standard-alphabet padded base64, bounded length, and exact decode/re-encode equality before returning owned bytes. Buffer is used only after grammar validation and cannot supply permissive acceptance. Both sanitized transcripts pass their identical `***not-base64***` fixture into both boundaries and assert rejection. The production-shaped packaged SRP worker executes the same malformed fixture rejection before reporting success; packaged Chromium positive and controlled-negative CSP tests passed. Candidate report SHA-256 is `7e9964f6998123ad736a2531dd6fcb7dd4af77d9dba60ede2c153d58c4684991`.
- Finding 2, actual sodium WASM identity/runtime: deterministic helper `scripts/ente-sodium-wasm-inventory.mjs` is shared by vector generation, controlled tests, output security, and `scan-build`. The scanner verifies the exact installed `libsodium-sumo@0.8.0` containing module and exactly one production-shaped packaged containing module, extracts exactly one canonical embedded payload, checks WASM magic, compiles it, sorts `WebAssembly.Module.imports/exports`, and compares module/payload/count/inventory digests against immutable reviewed `tests/fixtures/ente/sodium-wasm-approved.json` rather than regenerated vector data alone. Approved evidence SHA-256 is `fbe8ac917741694fa56404b5002ce8dd64ebc339c61e07d8ed94f4676e4e5447`; installed module SHA-256 is `71db3efbdef65b5bff9bc885b558a9414a1504e667ef36df95c7eec459fde3c1`; decoded WASM SHA-256 is `b15a381270ba8869e3a6b91d45e5a6439370c769e25ceef78aee9439f791d18f`; imports digest/count are `a5b5390f62925cc37c84edcd2117ddea27bc88b41c3f1cd143473be2744fe64d`/7; exports digest/count are `65af216ac8a046ac32237abc6c7eaa56af9be1ce03918b449e622515d573a0a6`/668. Production-shaped packaged containing module `enteSodiumWorkerEntry-CnY7lVdz.js` SHA-256 is `7e9d7626c594d15c0fc0e0f1f0b54e6d62f030577de19be026e5fbecb777ca7c`.
- The packaged browser worker intercepts the actual `WebAssembly.instantiate` bytes, independently compiles them, hashes the decoded bytes and sorted import/export inventories, and fails unless all three equal the committed approved values. Playwright asserts those exact runtime digests. Controlled scanner tests mutate loader recognition, add a second payload, corrupt WASM magic, and mutate the import inventory; every mutation fails the relevant extraction/inventory/allowlist boundary. Sodium vector fixture SHA-256 is `1b6fc6babae5be91f860211645deb306685155317483c9ec2b040687530eadc5`; scanner SHA-256 is `6a5ca2d1598a3300f4bbb8f88b9e0cf925d45a2f616f3d69add5dd36da93fb52`.
- Verification passed: focused final-finding suite 21/21; typecheck; zero-warning lint; full Prettier; dependency-cruiser zero violations; full Vitest 1,476/1,476 across 107 files; secure production build with output-policy 8/8 and scanner; packaged Chromium SRP/sodium positive and controlled-negative CSP 4/4; focused security/legacy 318/318; reproducible build 31/31 byte-identical files; production audit zero known vulnerabilities. Initial REDs were observed for missing SRP decoders and missing sodium inventory helper; one full-suite run exposed controlled scanner fixtures that intentionally omit sodium, corrected by restricting mandatory sodium identity enforcement to the real project root before the clean full pass.
- Environment limitation remains unchanged: commands ran on development Node `v24.18.0`, outside release Node `>=22.14.0 <23`, and Playwright Chromium 151 rather than release Chrome 110. These remain final-release environment gates.
- Disposition: `PASS-TASK12-PHASE1-EXACT-FINAL-TWO-REPLACEMENT-FINDINGS-CLOSED`. No Phase 2 and no Git operation.

## 2026-08-20 — Phase 2 consolidated RED

- Phase 1 prerequisite: implementation began only after the latest append-only Phase 1 authorization and the owner approval in this session. Historical STOP/PASS evidence above remains verbatim and is not reinterpreted.
- Strict TDD scope: added consolidated Phase 2 tests for fixed-origin transport, authenticated encrypted sync state and queue normalization, OTP-only libsodium entity framing, timestamp-ambiguity snapshot fallback, exact B/L/R merge classification, and durable-before-dispatch/uncertain-write behavior before adding production implementations.
- RED command: `pnpm exec vitest run apps/extension/test/background/ente-client.test.ts apps/extension/test/background/ente-sync-state.test.ts apps/extension/test/background/ente-otp-adapter.test.ts apps/extension/test/background/ente-read-engine.test.ts apps/extension/test/background/ente-merge.test.ts apps/extension/test/background/ente-write-engine.test.ts`.
- RED result: expected failure, 6/6 suites failed to import absent `client`, `sync-state`, `otp-adapter`, `read-engine`, `merge`, and `write-engine` production modules; zero tests executed. This is the consolidated RED baseline before Phase 2 production code.
- Guardrails: no Git, no production request, no content/popup/UI implementation, and no production manifest host permission or CSP authority change.

## 2026-08-20 — Phase 2 consolidated self-review blocking disposition

- Fresh self-review found a load-bearing integration failure: `installBackground` constructs `EnteSyncCoordinator` with a no-op cycle (`() => Promise.resolve()`), so manual/unlock/alarm triggers do not execute authentication, preflight pull, queue push, reconciliation, merge, or generation commit. Passing unit/static/build gates do not establish the requested end-to-end Phase 2 behavior.
- Additional blocking findings: the current Auth-key path retrieves only an existing key and has no create/race reconciliation; the approved design §4 explicitly lists Authenticator-key creation as a non-goal while the owner request asks for create/race reconciliation, requiring an authority decision rather than inventing an endpoint outside the fixed protocol table. The current state/adapter tests are a narrow initial baseline and do not cover the plan's complete interruption/root-race/conflict/write matrix.
- Verification evidence before this finding: consolidated RED failed 6/6 absent-module suites as expected; focused GREEN 171/171; full Vitest 1,506/1,506; security/legacy/Phase-1 regression 333/333; typecheck, lint, full Prettier, dependency-cruiser, secure build/output scanner, two-build reproducibility, and production audit passed on development Node v24.18.0.
- Disposition: `STOP-TASK12-PHASE2-INCOMPLETE-COORDINATOR`. Phase 2 is not approved and Phase 3 remains prohibited. Production manifest host permission and CSP remain unchanged; no real Ente request was made.

## 2026-08-20 — Phase 2 coordinator recovery remains STOP

- Root decision applied: Authenticator-key creation remains out of scope. The exact client allowlist still contains only `GET /authenticator/key`; a missing key now maps to fixed `ENTE_AUTH_KEY_MISSING`, the router returns only its protocol-safe fixed message, and no POST key path exists.
- Replaced the no-op implementation surface with a deterministic operational-cycle module that consumes only an opaque session epoch, strict OTP projections, encrypted state envelopes, endpoint-specific client methods, and injected crypto. It performs existing-key retrieval, bounded pull with snapshot fallback, strict remote projection parsing, B/L/R planning, durable attempt/uncertainty marking before one create/update/delete dispatch, post-write full pull, and root-change recomputation. Scheduler serialization remains one running cycle plus one coalesced follow-up; lock/cancel aborts awaits.
- Production startup remains deliberately unavailable until Phase 3 supplies manifest host authority and the fixed client/dependency factory. The runtime no longer uses a no-op: manual/unlock/alarm calls fail closed with `ENTE_UNAVAILABLE`, and automatic triggers consume that fixed failure without an unhandled rejection. No manifest host permission, production CSP, UI, or real-network authority was added.
- Genuine recovery RED: new coordinator integration tests failed 4/4 because `createEnteOperationalCycle` was absent. Focused recovery GREEN passed 187/187 across 22 files; runtime/router/platform subset passed 57/57. Typecheck, zero-warning lint, full Prettier, dependency-cruiser, complete Vitest, secure build/output scanner, security+legacy 317/317, two-build reproducibility, and production audit passed on development Node v24.18.0. The secure build initially detected Zod's dynamic `Function` graph after eager operational bundling; separating the injected Phase 2 cycle from the unavailable production scheduler closed the violation and the fresh secure build passed.
- Consolidated self-review does not authorize GREEN. The deterministic recovery suite proves full pull, fixed missing-key failure, durable-before-dispatch uncertain create/no blind retry, and external-root recomputation, but it does not yet prove the owner's complete requested matrix: successful create identity binding, update/delete post-observation finalization, complete conflict outcomes, unique/zero/multiple uncertain-create reconciliation, interruption at every stage/verify/preactivation/activation boundary, lock at every await, restart queue recovery, all concurrent-trigger cases, and exact one generation for items+journal+metadata through a real session-owned bridge. The production runtime is intentionally unavailable and therefore cannot establish the real dependency construction requested for an enabled service.
- Environment limitation remains: Node v24.18.0 is outside release Node `>=22.14.0 <23`; Chrome 110 release evidence was not run. No production request or Git operation occurred.
- Superseding disposition: `STOP-TASK12-PHASE2-RECOVERY-MATRIX-INCOMPLETE`. This entry supersedes only the implementation details of the prior no-op finding; Phase 2 remains unapproved and Phase 3 remains prohibited.

## 2026-08-20 — Phase 2 atomic-bridge remediation remains STOP

- Added a narrow `SessionService`-owned OTP plus Ente-metadata bridge. It exposes only opaque local revision, OTP items, and owned metadata bytes; retains journal, HOTP receipts, and unrelated encrypted metadata; stages, verifies, and activates items, journal observations, and `ente-otp-state` through exactly one active-root write; rejects stale opaque revisions without staging; and clears owned metadata buffers. Direct `SessionService` plus `FakeStoragePort` tests prove one generation, stale-revision no-write, and absence of plaintext item/metadata canaries in storage.
- Extended the injectable operational cycle with strict create-response identity capture, full-snapshot observation before create/update/delete finalization, encrypted before-snapshot identity evidence, observed-committed/absent-retry-authorized/divergent-conflict dispositions, retained conflict records, and no retry in the reconciliation cycle. Existing full-snapshot fallback, root-change recomputation, durable uncertainty-before-dispatch, and coordinator coalescing remain intact.
- Added an explicit session-cycle dependency boundary, and the production build remains fail-closed and tree-shakes that injectable boundary because Phase 3 network/manifest authority is prohibited. Secure output scan, dependency-cruiser, reproducibility, and production audit pass without hiding a production-reachable network graph.
- Verification on development Node v24.18.0: typecheck, zero-warning lint, full Prettier, dependency-cruiser, focused background/session tests, complete Vitest 1,511/1,511 across 108 files, secure build/output tests and scanner, reproducible build 31/31 files, and production audit with zero known vulnerabilities passed. The startup process harness was made deterministic by waiting for service-worker attachment before asserting the target.
- Consolidated self-review found that the owner's requested closure matrix is still not fully executable: the session-cycle boundary is not yet exercised by direct end-to-end `SessionService` operational-cycle tests; stage/verify/preactivation/active-root ambiguity and lock-at-each-await fault hooks are not covered for this new bridge; successful create and update/delete observation, every retained conflict outcome, unique/zero/multiple uncertain create, uncertain update/delete, restart recovery, and concurrent trigger coalescing are not all tested through the real bridge. Conflict resolution application is still separate from the operational cycle, and the retained conflict body does not yet encode complete B/L/R material needed to prove every resolution outcome.
- Environment limitations remain Node v24.18.0 rather than release Node >=22.14.0 <23 and no Chrome 110 release evidence. No production request, manifest host/CSP authority, Phase 3 feature, or Git operation occurred.
- Disposition: `STOP-TASK12-PHASE2-ATOMIC-BRIDGE-MATRIX-INCOMPLETE`. No GREEN entry is appended because the complete requested matrix is not proven; Phase 3 remains prohibited.

## 2026-08-20 — Phase 2 real-bridge evidence matrix GREEN

- Added the dedicated parameterized `ente-real-bridge.integration.test.ts`, which invokes the actual `SessionService`, `createEnteSessionCycleRepository`, `createEnteOperationalCycle`, and `EnteSyncCoordinator` with deterministic fake Ente transport and crypto. It proves create-response identity binding followed by a full pull; update/delete post-observation finalization; uncertain-create zero/one/multiple matching; uncertain update/delete observed/absent/divergent dispositions; queue survival across fresh session/coordinator instances; and concurrent-trigger coalescing to one running cycle plus one follow-up.
- Direct RED result before production repair: 19 tests executed, 9 failed and 10 passed. The failures exposed two production defects: delete planning duplicated an already-pending delete during preflight merge, and retained conflicts carried only one encrypted body and had no atomic application path. Separate expected REDs identified the missing awaited-boundary pause seam in `FakeStoragePort`; initial assertion/test-harness mistakes for successful uncertain-create and the exact active-root write ordinal were corrected without production changes.
- Production repairs were limited to directly exposed defects. Merge planning no longer enqueues a second local operation when one is already pending. Retained conflicts now persist encrypted base/local/remote projections plus remote identity, and `resolveEnteRetainedConflict` atomically applies keep local, accept remote, keep deletion, or apply remote deletion through the same session-cycle repository CAS commit. No production bypass, request authority, manifest, CSP, scheduler, UI, or Phase 3 behavior was added.
- The narrow testing seam adds one-shot before/after pause callbacks to `FakeStoragePort` operations. Parameterized actual-bridge fault tests inject before/after each of the seven generation writes (record, journal, metadata, manifest, verification marker, and active-root path as present for the fixture), covering stage, verification marker, preactivation, and active-root write-then-throw. They assert the old root before activation, at most and exactly one visible transition when activation writes, authenticated complete state for every visible root, no partially visible item/journal/metadata generation, and fail-closed locking on ambiguous activation. Await-boundary probes cover get/set before/after plus external-root notification while the real bridge is committing.
- Fresh focused command: `pnpm exec vitest run apps/extension/test/background/ente-real-bridge.integration.test.ts apps/extension/test/background/ente-coordinator.test.ts apps/extension/test/background/ente-sync-state.test.ts apps/extension/test/background/session-vault-repository.test.ts packages/storage --reporter=json --outputFile=/tmp/task12-focused.json`; result: 26/26 files, 177/177 tests, zero failures. The dedicated matrix itself passed 19/19.
- Fresh complete command: `pnpm exec vitest run --maxWorkers=1 --no-file-parallelism --reporter=json --outputFile=/tmp/task12-vitest.json`; result: 260/260 files, 1,524/1,524 tests, zero failures. Fresh `pnpm typecheck`, zero-warning `pnpm lint`, full `pnpm format:check`, and dependency-cruiser passed. Fresh `pnpm build:security` built production, passed 8/8 output-policy tests, and the build scanner passed.
- Environment limitation remains development Node v24.18.0 outside release Node >=22.14.0 <23; this closure did not claim final release Node/Chrome evidence. No Git operation or real Ente request occurred.
- Disposition: `GREEN-TASK12-PHASE2-REAL-BRIDGE-MATRIX-COMPLETE`. This supersedes only the immediately preceding Phase 2 matrix-incomplete disposition. Phase 3 remains prohibited and was not started.

## 2026-08-20 — Phase 2 validated review corrections

- Implemented all four validated Phase 2 review corrections without Phase 3, production API requests, or Git operations. Persisted explicit initial-snapshot/account binding; unmapped local creates are gated on an accepted complete snapshot for the same account; unsafe/missing bindings force a full read from sinceTime=0.
- Rewrote the read engine around strict server/entity timestamp domains, deterministic per-ID (updatedAt,id,digest) observations, exact repeat acceptance, later-observation application, same-time contradiction rejection, snapshot tombstone removal, aggregate live/change/page limits, actual 2,500-row boundary ambiguity, and exactly one incremental-to-full fallback. Added substantive multipage, 40-page, 100,000-change, and 10,000-live tests.
- Consolidated conflict authority around random expiring single-use capabilities bound to session epoch, root, conflict identity, and complete B/L/R digests; stale remote/local/root observations consume and reject the capability without applying a write. Public choices remain keep-local, keep-ente, and keep-both.
- Added connected-and-unlocked scheduler ownership: one exact 15-minute alarm, first sync only after both authorization conditions, cancellation/disarm on lock/disconnect/failure, stale alarm rejection, and no alarm extension after failed auth/sync. Runtime no longer arms the Ente alarm unconditionally.
- Genuine focused RED: read-engine tests failed 5/8 before the rewrite; sync-state binding tests failed 2/4 before schema/invariant changes. Focused GREEN: 7 files, 50/50 tests. Complete Vitest GREEN: 262/262 files, 1,540/1,540 tests. Typecheck, zero-warning lint, full format check, dependency-cruiser, secure production build/output scan, and production audit (zero known vulnerabilities) passed on development Node v24.18.0.
- Environment limitation remains: Node v24.18.0 is outside release Node >=22.14.0 <23; no Chrome 110 final-release evidence is claimed. Phase 3 remains prohibited and was not started.
- Disposition: GREEN-TASK12-PHASE2-VALIDATED-FOUR-REVIEW-CORRECTIONS.

## 2026-08-20 — Phase 2 correction evidence amendment

- Removed the service/router raw-resolution behavior: `ente.resolveConflict` now passes only the strict request capability plus public `keep-local | keep-ente | keep-both` choice to an injected capability resolver and fails closed when unavailable; it no longer converts resolution into a generic manual sync. Direct service tests cover this boundary.
- Final complete Vitest rerun after that correction: 266/266 files and 1,542/1,542 tests passed. Typecheck, zero-warning lint, and full format check also passed. The preceding dependency, secure-build/output, and zero-vulnerability audit evidence remains current for the same implementation except this source-only fail-closed service routing correction; Phase 3 remains prohibited.

## 2026-08-20 — Exactly three remaining Phase 2 findings closed

- Finding 1, production conflict resolution: added a runtime-owned `EnteConflictCapabilities` and `EnteConflictResolver` path wired through `EnteService`. Status/resolve responses expose only opaque capability plus `canKeepBoth`. Resolution consumes once and binds sender, session epoch, opaque root digest, conflict identity, and complete B/L/R digests; obtains a fresh complete snapshot through operational client/auth/crypto dependencies; recomputes conflict; maps keep-local, keep-ente, and keep-both deterministically including local/remote deletion; and commits through the atomic session-cycle repository CAS. Every stale check returns replacement previews without applying the requested write. The obsolete raw retained-conflict resolver was removed.
- Finding 2, runtime scheduler: the installed background now owns the exact 15-minute Ente alarm through live vault lock state and persisted runtime connection state. Connect/unlock authorizes the first sync only when both conditions hold; lock, disconnect, auth/sync failure, and dispose cancel/disarm; alarm handlers are epoch-bound so stale callbacks are ignored. Fake platform/storage integration covers runtime connect failure disarm, lock, unlock/restart re-arm, disconnect, stale alarm, and disposal behavior.
- Finding 3, incremental tombstones: incremental tombstones remain in the returned change set but only non-deleted entities count against `maxLiveRemoteEntities`; total change/page bounds still apply. Added a 12,500-tombstone, zero-live multipage regression alongside the 10,001-live rejection.
- TDD evidence: the tombstone test failed at the old `entities.size` live bound; the service sender-binding test failed because sender was not passed; runtime alarm integration failed because connect did not drive scheduler state; and scheduler failure-disarm failed because the alarm remained armed. Focused GREEN passed 44/44 tests across six suites, including service/capabilities, scheduler, read engine, installed background, and real session bridge.
- Full gates: typecheck, zero-warning lint, full Prettier, dependency-cruiser, and complete Vitest passed on development Node v24.18.0; complete Vitest reported 264/264 files and 1,541/1,541 tests. Secure production build remains blocked by the pre-existing production-reachable full-Zod `Function` provenance in `assets/otp-item-*.js` once the real operational runtime is wired; the scanner reports `function-reference`. A separate production audit completed with zero known vulnerabilities. This scanner finding is recorded rather than bypassed. Node v24 remains outside release Node >=22.14.0 <23.
- Scope: no Phase 3, manifest/host/CSP change, production request, or Git operation.

## 2026-08-20 — Phase 2 secure-build `function-reference` root cause and fix

- Reproduced without changing the scanner: `pnpm build:security` built 2,089 modules and failed the output CSP test with exactly `{ file: "assets/otp-item-BR_vuEBR.js", rule: "function-reference" }`. The emitted source contained full Zod's JIT capability probe `const e=Function; return new e("")`.
- Exact newly reachable chain: production background `main.ts` -> `ente/runtime.ts` -> `ente/session-cycle-repository.ts` -> `ente/sync-state.ts` -> bare `zod`. The same operational graph also reaches bare `zod` through `operational-cycle.ts` -> `read-engine.ts`/`client.ts` -> `schemas.ts`. `session-cycle-repository.ts` additionally imported the runtime `OtpItemSchema` from `@shardpass/domain`; source-map evidence showed the generated `otp-item` chunk contained Zod core plus Mini and domain `unicode-scalar-text.ts`, `item-metadata.ts`, `canonical-base32.ts`, and `otp-item.ts`. The domain implementation itself correctly imports `zod/mini`; the accidental Ente runtime schema import, not a bad domain subpath, made that full domain parser runtime-reachable.
- Fix: converted only Task12 Ente production schemas (`schemas.ts`, `sync-state.ts`, auth worker protocol, protocol/read-engine type boundaries) from bare full Zod to the established `zod/mini` strict-object/check/combinator pattern. Replaced the Ente repository's redundant `OtpItemSchema.parse` with a typed field projection because the remote projection is already strictly parsed and the atomic session repository remains the owning vault validation boundary. The domain import is now type-only. No scanner, CSP, manifest, authority, or exception changed.
- Prevention: added a source policy test covering both production Ente roots, requiring the type-only domain boundary and rejecting bare `zod`; added dependency-cruiser rule `ente-production-cannot-import-full-zod` so resolved Task12 Ente graph edges to `^zod$` fail independently of text spelling. Initial policy RED failed on `protocol.ts`'s bare-Zod type boundary before the implementation conversion.
- Output evidence: fresh `pnpm build:security` passed 8/8 output-policy tests and `scan-build`; emitted `assets/otp-item-Dt6Dwyi_.js` is 29,796 bytes with SHA-256 `804f730e3f4a8d7d17e2e6fb161a84870a5e43a6511912057b41742bb6fe67c8`. It contains Mini/core support but no `Function` identifier or function-constructor probe. The prior chunk was 53.68 kB; the fixed chunk is 29.10 kB.
- Verification on development Node v24.18.0: typecheck; zero-warning lint; full Prettier; dependency-cruiser (309 modules, 849 dependencies, zero violations); focused security/Ente 317/317; complete Vitest 266/266 files and 1,543/1,543 tests; secure production build/output scanner; reproducible build; production audit with zero known vulnerabilities. Node remains outside the release requirement `>=22.14.0 <23`.
- Disposition: `GREEN-TASK12-PHASE2-FUNCTION-REFERENCE-CLOSED`. No Phase 3 and no Git operation.

## 2026-08-20 — Phase 3 consolidated RED

- Owner authorization: Phase 2 was explicitly approved for this execution. Phase 3 follows the approved replacement design and current pin; every historical STOP and subsequent superseding entry above remains verbatim and authoritative in its recorded scope.
- Strict TDD RED command: `pnpm exec vitest run apps/extension/test/vault/EnteSettings.dom.test.tsx tests/security/manifest.test.ts tests/security/csp.test.ts`.
- RED result: expected failure. Ente settings module was absent; source manifest lacked exact `https://api.ente.io/*`; production CSP remained self-only rather than the exact approved `wasm-unsafe-eval` plus fixed Ente connect source. Three suites failed, with one unrelated source-entry assertion passing.
- Scope: no real account or production request, no custom server, no Git operation. Phase 3 implementation starts only from this recorded RED baseline.

## 2026-08-20 — Phase 3 consolidated self-review blocking disposition

- Strict RED was recorded before UI/authority implementation. Focused UI/security GREEN passed 32/32 tests; complete local Vitest passed 121/121 files and 1,549/1,549 tests on development Node v24.18.0. Typecheck, zero-warning lint, full Prettier, dependency-cruiser (313 modules/861 dependencies), secure build/output scan, two-build reproducibility, 17 legacy fixture checks, and production audit with zero known vulnerabilities passed. Packaged Chromium 151 sodium CSP positive/controlled-negative passed 2/2.
- Implemented metadata-only full-vault Ente settings, 18-state presentation, explicit conflict consequences, synchronous sensitive control clearing/stale-result suppression, summary-only popup, exact host permission/CSP, source/output network policy, credential canaries, scanner updates, dependency-bound evidence refresh, Task 12 scripts, and architecture/security documentation. No real Ente account or production request was used; no Git operation occurred.
- Consolidated review found unresolved load-bearing Phase 3 gaps. Production startup still calls `installBackground(createChromePlatform())` without concrete `EnteRuntimeDependencies`; connect UI creates an opaque job identifier but does not yet execute the dedicated auth worker and complete SRP/TOTP/Auth-key persistence cycle. The required deterministic packaged synthetic exact-endpoint matrix and mutation counter were not implemented. The broad packaged browser run also exposed pre-existing harness coupling plus new exact-name/screenshot drift; only isolated sodium CSP evidence is green.
- Official release evidence is unavailable: local Node v24.18.0 is outside `>=22.14.0 <23`, and Chromium 151 cannot substitute for Chrome 110. `verify-task12-release.mjs` correctly exits nonzero for official runtime/browser and absent final-pass evidence.
- Disposition: `STOP-TASK12-PHASE3-RUNTIME-BROWSER-MATRIX-INCOMPLETE`. Do not claim Task 12 complete or release-ready. Production authority is present but the settings path fails closed because no runtime dependency owner is installed; no production network request was made.

## 2026-08-20 — Phase 3 recovery implementation and remaining blocking disposition

- Recovered from the preceding STOP without rewriting any historical line. Production startup now constructs and installs concrete fixed-origin client/fetch, approved libsodium, SRP auth-worker, encrypted generation repository, operational cycle, conflict resolver, coordinator, and scheduler dependencies. Runtime startup no longer waits for libsodium initialization, so the ordinary foundation router remains available while approved crypto initializes fail-closed.
- The full-vault connect flow now moves owned email/password bytes directly to the dedicated packaged local auth worker, clears controls and owned buffers, performs Argon2id/login-subkey/SRP proof verification, supports Ente TOTP2FA continuation, recovers token/master-key material, and sends only a bounded opaque encoded session result through the trusted vault/background message boundary. A successful background activation places credential and key envelopes inside authenticated encrypted generation metadata before the connected trigger; malformed/replayed/failed activation does not arm scheduling. Password and TOTP values are absent from runtime messages.
- Production packaging now includes the fixed auth-worker entry and audited SRP/libsodium graph. Output inventory/scanners recognize only the exact reviewed auth/background fixed-origin sinks, retain exact libsodium WASM identity checks, reject compatibility leakage elsewhere, and pass the secure build. Exact host permission and CSP remain unchanged and correct.
- Browser harness recovery fixed SRP harness construction ordering, startup blocking, exact accessible-name drift, and intentional Ente-aware popup/vault/backup/OTP coexistence screenshot changes. Complete packaged Chromium 151 suite passed 30/30, including positive/negative sodium and SRP CSP, startup, migration, setup/unlock, backup, OTP CRUD/import/autofill, lock/restart behavior, accessibility, and encrypted persistence. Full Vitest passed 1,552/1,552 across 268 files; typecheck, zero-warning lint, full Prettier, dependency-cruiser (315 modules/872 dependencies), secure build/output scan, reproducible build, and production audit with zero vulnerabilities passed on development Node v24.18.0.
- Remaining load-bearing gap: the required single deterministic packaged exact-`https://api.ente.io` route-interception matrix with request/mutation counter was not completed. Existing real-bridge deterministic tests cover pull/create/update/delete/conflict/uncertain/restart/scheduler semantics, but they are not the required real packaged UI/runtime endpoint matrix and cannot substitute for it.
- Official release blockers remain external environment evidence: local Node v24.18.0 is outside `>=22.14.0 <23`, and Chromium 151 is not Chrome 110. `node scripts/verify-task12-release.mjs` correctly exits nonzero for official Node and absent Phase 3 final PASS; production audit reports no vulnerabilities. No real Ente request, credentials, Git operation, or production API use occurred.
- Disposition: `STOP-TASK12-PHASE3-PACKAGED-MOCK-MATRIX-INCOMPLETE`. Runtime/auth/startup/browser-regression gaps from the prior STOP are closed, but Task 12 is not complete or release-ready until the packaged exact-endpoint matrix and official Node 22/Chrome 110 evidence pass. No `PASS-TASK12-PHASE3-FINAL` is recorded.

## 2026-08-20 — Phase 3 recovery self-review correction

- The preceding recovery entry overstates closure of the reusable-session transfer boundary. The implemented worker result is bounded and password-free, but it is base64url-encoded JSON containing reusable token/master-key material, not ciphertext or an owned transferred `ArrayBuffer`. It therefore does not satisfy the requested one-use document/session-bound encrypted transfer capability and violates the prohibition on reusable token/key JSON strings.
- The auth worker also performs fixed-origin authentication fetches directly. Although fixed-origin policy and packaged scanning contain that authority, the final architecture review must decide and implement the requested background-fetch handoff without exposing password material, then prove one-use sender/document/session binding, replay rejection, immediate background encryption/ownership, and buffer cleanup.
- Corrected disposition: `STOP-TASK12-PHASE3-SESSION-TRANSFER-AND-PACKAGED-MOCK-MATRIX-INCOMPLETE`. Production startup and broad regressions are improved, but the reusable-session transfer architecture and packaged exact-endpoint matrix remain load-bearing implementation blockers in addition to official Node 22/Chrome 110 evidence.

## 2026-08-20 — Phase 3 blocker follow-up evidence; no final review claim

- Systematic migration diagnosis reproduced the packaged flake in focused repeats and in complete-suite/release-verifier context. The production setup challenge lifetime remains exactly 300,000 ms and the packaged KDF executor remains fail-closed at 120,000 ms; neither product value changed. The observed failure was a contended packaged Argon2 operation whose worker message/timer could remain pending beyond the test's prior locator timeout, compounded by broad in-page `Worker` replacement and setup while legacy source records were already installed.
- Harness correction creates the local vault before installing legacy source records, installs migration instrumentation only afterward, instruments workers with a transparent `Proxy`, identifies only the exact same-origin `/assets/legacy-kdf-worker-[hash].js` URL, and observes cancellation through Playwright's exact page-worker close event. Setup now condition-polls success versus the sanitized worker failure and, on an ambiguous bounded timeout, reloads to obtain a fresh document-bound challenge before one UI retry. No auth TTL, KDF work factor, count expectation, production code, or network authority was loosened.
- Focused packaged migration evidence: 5/5 repeated passes after source/setup ordering, then 3/3 passes with the bounded reload path. Combined focused migration plus Ente matrix passed 4/4 over two repeats. A final complete built packaged Chromium 151 suite passed 31/31 in 5.7 minutes.
- The deterministic exact-origin packaged Ente mock now fails unknown methods/paths, validates authority, headers, diff query and token, computes exact SRP/key recovery, maintains deterministic entity/diff state, implements POST/PUT/DELETE mutation endpoints, and records exact mutation bodies/counts. The real packaged UI/runtime test proves login/key recovery/full pull, queues a local create with zero premature mutations, dispatches exactly one POST on explicit sync, observes a subsequent diff, and rechecks encrypted storage against password/token/master/auth-key canaries before lock cancellation.
- Local implementation gates passed on development Node v24.18.0: typecheck, zero-warning lint, full Prettier, dependency-cruiser (317 modules/880 dependencies), secure build and 8/8 output tests/scanner, complete Vitest 1,551/1,551 across 122 files, complete packaged browser 31/31, reproducible build, and production audit with no known vulnerabilities. `verify-task12-release.mjs` ran all local evidence successfully and stopped only at `official Node >=22.14.0 <23 evidence is required`; official Chrome 110 evidence is likewise not supplied by local Chromium 151.
- Scope and disposition: synthetic interception only; no real Ente request, credentials, Git operation, production TTL change, or final review claim. The broader requested packaged conflict/deletion/uncertain-restart/disconnect/scheduler scenario expansion and the previously recorded session-transfer architecture finding are not closed by this follow-up. No `PASS-TASK12-PHASE3-FINAL` is recorded.

## 2026-08-20 — Superseding one-use session-handoff finding

- This append-only entry supersedes the reusable-session transfer finding at lines 273-277. The current production auth handoff is a libsodium sealed box over a strict owned binary payload; repository/source search finds no `encryptedSession`, reusable token/key JSON field, or base64-encoded reusable session object in the production implementation. Base64 remains only inside Ente's external wire/storage formats, not as the UI-to-background reusable session handoff.
- Exact tests cover strict binary encode/decode without JSON/base64, sealed-box confidentiality, sender extension/context/URL/document binding, vault session-epoch binding, active-root digest binding, exact 60,000 ms TTL expiry, destructive one-use consumption, replay rejection after success and after a wrong-document attempt, and explicit store cleanup. Consumption removes the capability before validation/decryption and clears the ephemeral public/private keys in `finally`; plaintext and caller-owned test buffers are cleared after use.
- This correction closes only the stale architectural finding. The packaged mutation/conflict/uncertain-restart/lock/disconnect/scheduler matrix and release-environment gates remain open; no final approval or release claim is made.

## 2026-08-20 — Phase 3 expanded packaged pre-review evidence

- Expanded the existing strict deterministic exact-origin packaged Ente mock through the real built vault UI, auth worker, message router, background runtime, encrypted generation repository, operational cycle, client fetch, and Chrome alarms adapter. The focused scenario passed and the complete packaged Chromium 151 suite passed 31/31 in 5.6 minutes; the reviewed compact connected screenshot and serious-accessibility scan remain green.
- Exact packaged endpoint/mutation evidence now covers SRP attributes/create-session/verify-session, Auth-key recovery, incremental/full entity diff, queued create with zero premature writes, exact POST dispatch plus observation, real editor update with exact PUT dispatch plus full observation, and real delete dialog with exact DELETE query dispatch plus tombstone observation. The strict route rejects unknown methods/paths and validates exact authority, `Accept`, mutation `Content-Type`, authenticated-only `X-Auth-Token`, canonical diff query, and mutation bodies. Mutation count is exactly four across create/update/delete and one uncertain committed create.
- The uncertain mutation response is forced only after the mock commits the create and then resets the connection. The real runtime records failure, a fresh packaged document performs a complete `sinceTime=0` reconciliation, and the exact mutation count remains four, proving no duplicate create. Storage canaries are rechecked against password, token, master key, Auth key, and `encryptedSession`; none appears in serialized local storage.
- Packaged runtime proves one exact connected+unlocked alarm with `periodInMinutes: 15`, and lock removes it; a subsequently created stale same-name alarm produces no pull or mutation. Scheduler unit/integration tests separately invoke the exact captured alarm callback and prove one sync, coalescing, lock/disconnect/error cancellation, and epoch-based stale callback rejection. Real bridge tests cover fresh `SessionService`/coordinator restart reconciliation without duplicate dispatch, all zero/one/multiple uncertain-create observations, update/delete uncertainty consequences, authenticated generation commits, and lock at every awaited storage boundary.
- Conflict resolution remains exercised through the real repository/client/crypto bridge and reachable production conflict UI: explicit keep-local, keep-Ente, and keep-both choices are capability/session/root/B-L-R bound; keep-local queues update or deletion, keep-Ente accepts remote replacement or deletion, and keep-both is disabled for deletion conflicts because it cannot preserve two live values. Disconnect UI is explicit local-only; service/runtime tests prove credential envelope, mappings, pending/uncertain/conflict metadata and scheduler authority are cleared without issuing remote deletion.
- Local implementation evidence passed on Node v24.18.0: typecheck; zero-warning lint; full Prettier; dependency-cruiser (317 modules/880 dependencies, zero violations); secure build plus 8/8 output tests and scanner; complete Vitest 1,551/1,551 across 122 files; focused security+legacy 320/320; complete packaged browser 31/31; two-build reproducibility across 38 files; production audit with no known vulnerabilities. The release verifier reran its complete local evidence gate and exited only for `official Node >=22.14.0 <23 evidence is required`; local Chromium 151 is not official Chrome 110 evidence.
- Pre-review disposition only: implementation and local evidence are green, but no `PASS-TASK12-PHASE3-FINAL`, final approval, or release-ready claim is recorded. Required Node 22 and Chrome 110 execution remains external. No real Ente request/credential, Git operation, or production authority/TTL relaxation occurred.

## 2026-08-20 — Final Task 12 review remediation evidence; no final approval

- Atomic local disconnect now cancels the active coordinator, clears one-use auth handoffs and conflict capabilities, and atomically activates one encrypted generation preserving the complete local OTP set while removing `ente-otp-state` entirely. No credential/master/Auth envelopes, fingerprint, mappings, bases, cursor, pending, uncertain, conflicts, reauth, or connected scheduler state remains parseable after disconnect; an absent metadata value restarts as disconnected. The disconnect request no longer depends on an unrelated capability, and the UI terminates the auth worker and clears all sensitive controls on cancel/error/unmount. No disconnect network authority or remote mutation was added.
- One exact cycle-wide response budget now covers Auth-key retrieval, all incremental pages, ambiguity fallback snapshot pages, mutation responses, and post-mutation full pulls. The fixed-origin client checks remaining budget before dispatch, rejects declared overflow before body read, charges exact `arrayBuffer` bytes before UTF-8/JSON decode, and never resets the budget on fallback. Focused REDs were observed for the absent budget API and fallback plumbing; focused GREEN passed 33/33 before the broader gates.
- The real packaged TOTP 2FA path is now deterministic evidence: SRP returns `twoFactorSessionID`; the UI renders and clears the one-time-code control; the worker sends exact `POST /users/two-factor/verify` body `{code:"123456",sessionID:"10000000-0000-4000-8000-000000000003"}`; only the returned encrypted token/key attributes lead to Auth-key retrieval and the initial pull. The strict mock still rejects unknown methods and routes, so OTT/passkey/unknown fallback is absent. Focused packaged Chromium 151 passed 2/2 for TOTP and the existing SRP/mutation/restart/alarm matrix.
- Final local release-verifier evidence on development Node v24.18.0: typecheck, zero-warning lint, full Prettier, dependency-cruiser (317 modules/882 dependencies), secure build/output tests/scanner, complete Vitest 1,555/1,555 across 122 files, complete packaged Chromium 151 32/32, reproducible build across 38 files, and production audit with no known vulnerabilities all passed. The wrapper command itself rejects its configured local-command value; direct `verify-task12-release.mjs` completed the full local evidence matrix and exited only for `official Node >=22.14.0 <23 evidence is required`. Chrome 110 evidence also remains external.
- Evidence only: no `PASS-TASK12-PHASE3-FINAL`, final approval, or release-ready claim is recorded. No Git operation, real Ente API request, real credential, or remote disconnect mutation occurred.

## 2026-08-20 — Final pre-review command and packaged-runtime remediation; no final approval

- Reproduced `pnpm verify:project1:task12` failing with `Invalid local verification command.` Root cause was script drift: Task 12's exact local command was absent from `check-engine.mjs`, while the local script bypassed the release verifier and ran evidence directly. The exact Task 12 command is now allowlisted with the correct requirement label; official Node 24 execution fails before evidence with the useful exact local command, unknown commands remain rejected without reflection, and the local Node 24 command runs the same release verifier/evidence path as official execution.
- The packaged Ente matrix exposed a real stale-alarm callback defect: `main.ts` captured `scheduler.alarmHandler()` before the scheduler was armed, permanently binding the pre-arm epoch. The runtime now obtains the epoch-bound callback when the real `chrome.alarms.onAlarm` event fires. Packaged Chromium proves one real connected/unlocked alarm event causes exactly one additional diff pull; after local disconnect, a same-name real alarm produces no pull. Disconnect is inspected through the safe `ente.status` message surface only: disconnected, zero pending/conflicts, and no reusable credential/mapping fields; local OTP remains and disconnect makes no Ente request. UI confirmation state now closes once runtime connection state becomes disconnected.
- Packaged conflict testing audit found keep-local/keep-Ente/keep-both remained direct repository/client/crypto and UI-component evidence rather than complete packaged choice execution. Deterministic packaged experiments identified timing/cursor setup hazards; unsupported claims were not retained as passing evidence. The existing packaged matrix remains focused and stable while direct conflict semantics continue to cover local/remote deletion and keep-both behavior. This is an explicit pre-review evidence limitation, not an approval claim.
- Final development-only Node v24.18.0 local command completed typecheck, zero-warning lint, full Prettier, dependency-cruiser (317 modules/882 dependencies), secure build with 8/8 output tests and scanner, complete Vitest 1,558/1,558 across 122 files, packaged Chromium 151 32/32 in 6.3 minutes, reproducible build across 38 files, and production audit with no known vulnerabilities. The release verifier then stopped only at `official Node >=22.14.0 <23 evidence is required`. Official `pnpm verify:project1:task12` separately stopped at the engine gate before evidence and printed `verify:project1:task12:local-node24`. Chrome 110 evidence remains external.
- Evidence only: no `PASS-TASK12-PHASE3-FINAL`, final approval, or release-ready claim is recorded. No Git operation, real Ente API request, real credential, or production API use occurred.

## 2026-08-20 — Four final Task 12 review blocker remediation evidence; no final approval

- Corrected keep-both semantics through the real encrypted `SessionService` cycle repository: the existing local identity retains L and becomes local-only with its mapping/base removed; one fresh local identity receives R and the existing remote mapping/base; pending remains exactly empty and resolver application issues no POST/PUT/DELETE. Local- or remote-deletion keep-both capabilities report `canKeepBoth: false`, reject application, preserve the complete pre-resolution generation, and issue no mutation. Focused RED reproduced the prior reversed identities/create queue; focused GREEN passed exact items, identities, mappings, bases, conflict removal, queue-zero, and network-zero assertions.
- Conflict resolution now creates one `EnteResponseBudget` for the complete resolve attempt and passes that same object through Authenticator-key retrieval and the forced complete `sinceTime=0` page read. A deterministic 40 MiB key response plus individually valid 25 MiB page exceeds the 64 MiB cumulative limit before repository commit; the exact pre-resolution generation remains active.
- PUT/DELETE success handling now validates bounded aggregate headers and canonical decimal Content-Length, asserts declared remaining capacity before read, reads and charges exact body bytes against the per-response and shared budget, and accepts only exactly zero body bytes. Nonzero body mutation is protocol drift; oversized or malformed declarations are rejected before body read. Focused client RED failed all three new assertions before implementation and GREEN passed 8/8.
- Real resolver bridge coverage now uses the production `EnteConflictResolver`, `EnteConflictCapabilities`, encrypted session repository, fixed client/crypto interfaces and complete forced snapshot freshness. Existing service/router/runtime/session-handoff tests retain sender/document/session/root/B-L-R digest binding, one-use/replay/staleness, and generation atomicity coverage. The strict packaged synthetic exact-origin matrix was rerun green 2/2 and the complete packaged suite green 32/32; unsupported experimental packaged conflict claims were not retained.
- Final development-only `pnpm verify:project1:task12:local-node24` evidence passed typecheck, zero-warning lint, full Prettier, dependency-cruiser (317 modules/885 dependencies, zero violations), secure build/output 8/8 and scanner, complete Vitest 122/122 files and 1,565/1,565 tests, complete packaged Chromium 151 32/32, reproducible build across 38 files, and production audit with no known vulnerabilities. The verifier then correctly exited nonzero only for `official Node >=22.14.0 <23 evidence is required`; Chromium 151 is not Chrome 110 evidence.
- Evidence only: no `PASS-TASK12-PHASE3-FINAL`, final approval, release-ready claim, Git operation, real Ente request, or real credential use occurred.

## 2026-08-20 — Exact final conflict execution evidence; no final approval

- Added direct parameterized encrypted-session resolver integration through the production `EnteConflictResolver`, `EnteConflictCapabilities`, `SessionService` cycle repository, client/crypto interfaces, preview, and resolve. The four successful cases prove: keep-local live retains the same local ID and L plus mapping/base while replacing prior local work with exactly one update; keep-local local deletion retains mapping/base and queues exactly one delete; keep-Ente live replaces that same local ID with R and updates mapping/base with no pending work; keep-Ente remote deletion removes item/mapping/base with no pending work. Every case forces one fresh complete `sinceTime=0` read, performs exactly one active-root generation, issues zero POST/PUT/DELETE during resolution, consumes the capability once, and rejects replay without a second pull or generation.
- Retained capability tests cover session epoch, root digest, conflict identity, complete B/L/R digest, expiry, one-use consumption, and stale local/remote/root rejection. Deletion previews expose `canKeepBoth: false`; existing direct keep-both evidence retains L on the existing local-only identity and creates exactly one fresh local identity for R with its mapping/base, zero pending work, and zero network mutation.
- Fixed operational conflict retention so a detected three-way conflict removes superseded pending work for that local identity before storing B/L/R. Added a direct queued-B→L plus remote-R refresh test proving conflict retention and no dispatch. The strict packaged mock now emits canonical encrypted OTP plaintext for out-of-band remote edits, matching the production parser rather than failing as invalid data.
- Added five isolated deterministic packaged Chromium scenarios through the real built UI/router/runtime/session/client/crypto: actual create B, local L edit or local deletion, remote R edit or remote deletion, conflict refresh, then explicit keep-local, keep-Ente, or keep-both. The UI proves no default selection, deletion disables keep-both, post-resolution local rows are exact after a fresh document read, safe status returns zero conflicts/pending after the resolution cycle, and the mock records exactly one setup POST with zero PUT/DELETE during conflict refresh/resolution. Each scenario attaches a full-page post-resolution screenshot.
- Verification passed: typecheck; zero-warning lint; full Prettier; dependency-cruiser (317 modules/885 dependencies); focused resolver/service/capability 34/34; full background 332/332 before the final added refresh case and focused real bridge 24/24 after it; secure build/output 8/8 plus scanner; full packaged strict Ente matrix 7/7 serial; reproducible build; production audit with zero vulnerabilities. A concurrent broad `test:project1:task12` run was not accepted as green because build-dependent SRP inventory was absent during that run and two CPU-heavy tests timed out (65/68 files, 826/829 tests); the subsequent ordered secure build and focused/browser gates passed. Development Node is v24.18.0, outside the official >=22.14.0 <23 requirement, and packaged Chromium is not official Chrome 110 evidence.
- Evidence only: no `PASS-TASK12-PHASE3-FINAL`, final approval, or release-ready claim is recorded. No Git operation, real Ente API request, real credential, or production API use occurred.

## 2026-08-21 — Release-evidence parser hardening; blockers retained

- Replaced Task 12 release gating based on arbitrary ledger substring inclusion with exact anchored Phase 1/Phase 2 disposition lines plus strict canonical JSON records for Chrome 110 and the standalone final disposition. Structured records reject malformed or duplicate JSON, unknown fields, future timestamps, candidate manifest hash/name/version mismatch, wrong kind/status/version/command/result, and multiple-record containers. Official paths remain fixed to the repository ledger, manifest, and `.sdd` evidence filenames; temporary synthetic evidence is accepted only by parser tests.
- Focused parser tests passed 9/9, including negative historical prose (`No PASS...`, `Chrome110 remains external`), substring injection, malformed/duplicate/unknown/future/hash-mismatch rejection, exact anchored phase records, and a valid synthetic parser record. Typecheck, zero-warning lint, full repository Prettier, and all tooling tests passed (9 files, 49 tests) on development Node v24.18.0.
- The complete development-only Node 24 command subsequently passed dependency-cruiser (317 modules/885 dependencies), secure output tests 8/8, full Vitest 123/123 files and 1,580/1,580 tests, packaged browser 37/37, reproducibility across 38 files, and production audit with no known vulnerabilities. It then exited nonzero with the honest blockers: actual process v24.18.0 instead of Node 22, invalid/absent official Chrome 110 structured evidence, and invalid/absent final review structured disposition.
- Evidence only: no positive Chrome 110 or final-disposition JSON file was created. Official Node 22 process evidence, official Chrome 110 structured evidence, and final review structured disposition remain required; no `PASS-TASK12-PHASE3-FINAL`, final approval, release-ready claim, or Git operation is recorded.

## 2026-08-21 — Final Task 12 release-tooling hardening; blockers retained

- Replaced legacy root-manifest identity with a deterministic complete packaged-candidate digest over every sorted relative regular non-symlink `dist` file name and its exact bytes, including the packaged manifest, assets, pages, and generated production-graph audit metadata. Release evidence now records schema v2 `candidateDigest` plus the packaged manifest name/version, and verification recomputes that identity from the current clean `dist`; wrong names/bytes, symlinks, extra/missing files, and legacy `1.2.1` substitutions fail closed.
- Added production commands `verify:project1:task12:chrome110` and `verify:project1:task12:final-review`. The Chrome command requires one explicit absolute executable, reads its actual version and requires major exactly 110, clean-builds and security-scans `dist`, runs the exact complete packaged browser suite and security suite through the explicitly supplied Playwright browser location, confirms candidate immutability, and exclusively/atomically writes canonical evidence only after success. The final-review command requires separate reviewer-supplied canonical approval and detached signature files; validates approved status, external reviewer identity/organization, candidate digest, Chrome-evidence digest, explicit Chrome/security/browser pass results, and timestamp; then exclusively/atomically writes the final evidence. Neither command accepts a self-computed version override or overwrites existing evidence.
- Focused Task 12 release tests passed 7/7 and complete tooling tests passed 47/47. Typecheck, zero-warning lint, full Prettier, dependency-cruiser, secure build/output scan, full Vitest 123/123 files and 1,578/1,578 tests, complete packaged browser suite, reproducibility across 38 files, and production audit with no known vulnerabilities passed on development Node v24.18.0. The local release verifier then correctly failed only on Node 22, absent Chrome 110 evidence, and absent external final-review evidence. Direct production Chrome/final-review command invocations both exited nonzero and wrote no evidence files in the current environment.
- Evidence only: no positive Chrome 110 or final-disposition JSON file was created or appended. No `PASS-TASK12-PHASE3-FINAL`, reviewer approval, release-ready claim, Git operation, real credential, or real Ente request is recorded.

## 2026-08-21 — External final-review signature trust hardening; blockers retained

- Replaced report-asserted reviewer trust with a strict owner-provisioned canonical trust store. The committed template contains zero keys; accepted entries require a unique exact reviewer ID/organization and SHA-256 SPKI fingerprint mapping, active status, current not-before/not-after validity, and an Ed25519 PEM/SPKI or exact 32-byte raw public key. Empty/missing stores, revoked/inactive/expired/future, duplicate identity/key, malformed/non-Ed25519 keys, fingerprint mismatch, unknown keys, random signatures, altered report bytes, and report identity mismatch fail closed. No trusted public key was added.
- The final-review producer now requires explicit report, detached signature, and trust-store paths; parses canonical report bytes, verifies the detached Ed25519 signature over those exact bytes with Node crypto, derives reviewer identity from the matched trusted key, checks approved candidate/Chrome/status/tool bindings, and exclusively/atomically writes disposition plus immutable hash-named report/signature evidence. Disposition records relative evidence paths, report/signature hashes, and reviewer key fingerprint. The release verifier independently loads the configured trust store and preserved evidence, re-verifies exact bytes/signature/trust/identity/status/candidate/Chrome bindings and hashes, rather than trusting the disposition alone.
- Focused trust/release tests passed 12/12 with ephemeral temporary Ed25519 keys. Typecheck, zero-warning lint, full Prettier, dependency-cruiser (317 modules/885 dependencies), secure build/output scan, full Vitest, complete packaged browser suite, reproducibility across 38 files, and production audit with no known vulnerabilities passed in the full local evidence gate on development Node v24.18.0. The development release gate correctly exited nonzero only for official Node 22, absent official Chrome 110 evidence, and absent trusted external final-review disposition; the empty committed trust template did not approve anything.
- Evidence only: no positive Chrome 110, report, signature, or final-disposition evidence was created. Owner key provisioning and independent review remain external blockers. No `PASS-TASK12-PHASE3-FINAL`, self-approval, release-ready claim, real trusted key, positive evidence, or Git operation is recorded.
